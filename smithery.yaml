# Generated by static-site/registry_publisher.py from the live server.json.
# Safe to commit: carries no tool description, so no A/B cohort's copy is
# frozen here. Re-generated every 3h generation cycle.
name: "com.reqbeat/hiring-signals"
title: "Reqbeat Hiring Signals"
description: "Find companies hiring for a role and geo, qualify them, and watch them for changes."
remote:
  type: "streamable-http"
  url: "https://mcp.reqbeat.com/mcp"
  headers:
    - name: "X-API-Key"
      required: true
      secret: true
